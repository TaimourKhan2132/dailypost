const KEY="dailypost_stories_v1";const seed=[{title:"A slower morning can change the whole day",author:"DailyPost Editorial",body:"There is value in making room for a few quiet minutes before the day becomes loud.",color:"coral",views:128},{title:"What makes a story worth reading?",author:"DailyPost Editorial",body:"Good stories make us notice something we had passed by before.",color:"blue",views:96},{title:"Small ideas can travel far",author:"DailyPost Editorial",body:"A simple thought, clearly shared, can start a conversation far beyond its first reader.",color:"green",views:74}];
const get=()=>{try{return JSON.parse(localStorage.getItem(KEY))||seed}catch{return seed}};const save=x=>localStorage.setItem(KEY,JSON.stringify(x));const esc=s=>String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));
function render(){let s=get().sort((a,b)=>(b.created||0)-(a.created||0));document.querySelector("#storyCount").textContent=`${s.length} stor${s.length==1?"y":"ies"}`;document.querySelector("#stories").innerHTML=s.map(x=>`<article class="story"><div class="story-art ${x.color}">${esc(x.title[0].toUpperCase())}</div><div class="story-content"><h3>${esc(x.title)}</h3><p>${esc(x.body)}</p><small>By ${esc(x.author)} · ${Math.max(1,Math.ceil(x.body.split(/\s+/).length/180))} min read</small></div></article>`).join("");let m=s.slice().sort((a,b)=>(b.views||0)-(a.views||0))[0];if(m)document.querySelector("#mostReadTitle").textContent=m.title}
document.querySelector("#storyForm").addEventListener("submit",e=>{e.preventDefault();let s=get();s.unshift({title:title.value.trim(),author:author.value.trim(),body:body.value.trim(),color:color.value,created:Date.now(),views:0});save(s);e.target.reset();render();alert("Your story has been published in this browser.");location.hash="read"});
document.querySelector("#newsletterForm").addEventListener("submit",e=>{e.preventDefault();alert("Thanks — you're subscribed in this demo.");e.target.reset()});
const modal=document.querySelector("#searchModal"),input=document.querySelector("#searchInput"),results=document.querySelector("#searchResults");document.querySelector("#searchOpen").onclick=()=>{modal.hidden=false;input.focus()};document.querySelector("#searchClose").onclick=()=>modal.hidden=true;modal.onclick=e=>{if(e.target===modal)modal.hidden=true};input.oninput=()=>{let q=input.value.toLowerCase().trim();results.innerHTML=q?get().filter(s=>(s.title+" "+s.body+" "+s.author).toLowerCase().includes(q)).map(s=>`<div class="result"><strong>${esc(s.title)}</strong><br><small>By ${esc(s.author)}</small></div>`).join(""):""};render();